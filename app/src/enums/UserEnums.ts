export enum UserRole {
  MENTOR = "mentor",
  STUDENT = "student",
  ADMIN = "admin",
}

export enum FormsOfEducation {
  MENTOR = "mentor",
  STUDENT = "student",
  ADMIN = "admin",
}

export enum TokenExpiration {
  ACCESS = 60 * 60,
  REFRESH = 356 * 24 * 60 * 60,
}
